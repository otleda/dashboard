import styled from 'styled-components';

export const Container = styled.div`
    > select {
        padding: 6px 10px;
        border-radius: 3px;
        border: none;
        margin-left: 1em;
        color: #252A4B;
    }    
`;